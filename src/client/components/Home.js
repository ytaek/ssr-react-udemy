import React from 'react';

const Home = () => {
	return (
		<div>
			<div>I'm very very the another home component</div>
			<button onClick={ () => console.log('Hi there!')}>Press me!</button>
		</div>
	);
};

export default Home;
